# GENERATED by lock-requirements.sh — do not edit by hand.
# Edit requirements.in and re-run ./lock-requirements.sh.
#
# The full closure is pinned, transitive dependencies included, so the
# image built today and the image built in six months install the same
# code. An unpinned transitive dependency is still an unpinned
# dependency: it is simply one nobody reads.

# ── Direct ────────────────────────────────────────────────────
ezdxf==1.4.4
ifcopenshell==0.8.5
numpy==2.5.2
pdfplumber==0.11.10
pillow==12.3.0
pypdf==6.16.2
pypdfium2==5.13.0
pytesseract==0.3.13

# ── Transitive (pulled in by the above) ───────────────────────
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
fonttools==4.63.0
isodate==0.7.2
lark==1.3.1
packaging==26.3
pdfminer.six==20260107
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
shapely==2.1.2
six==1.17.0
typing_extensions==4.16.0
